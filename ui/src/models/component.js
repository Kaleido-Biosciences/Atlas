export const createComponent = (
  id,
  type,
  name,
  description,
  options,
  tooltip,
  color,
  data
) => {
  return {
    id,
    type,
    name,
    description,
    options,
    tooltip,
    color,
    data,
  };
};
